[user]
    name = Garrett Doorenbos
[diff]
    tool = meld
[difftool]
    prompt = false
[difftool "meld"]
    cmd = meld "$LOCAL" "$REMOTE"
[merge]
    tool = meld
[mergetool "meld"]
    # Choose one of these 2 lines (not both!)
    cmd = meld "$LOCAL" "$MERGED" "$REMOTE"
    #cmd = meld "$LOCAL" "$BASE" "$REMOTE"
[credential]
    helper = cache --timeout=3600
